__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.venv/

# Reverse-engineering scratch — never commit (VW copyright / large binaries)
*.apk
*.xapk
*.dex
decompile/
jadx*/
*.jdk/
jdk-*/
/vendor-decompiled/

# Claude Code local session/config
.claude/*
!.claude/skills/
manuals/    # copyrighted VW PDFs — never commit (citations only)

# VW app-derived assets — icons/drawables/logos are VW/partner copyright (citations only;
# regenerate locally from the APK, see ui/README.md). Purged from history 2026-07-08.
ui/assets/svg/
strings_resolved*
*.strings

# BLE / HCI capture files (own-vehicle captures — never commit)
*.cvr
*.pcap
*.pcapng
*.pklg
*.btsnoop

# other Android build artifacts (siblings of the ignored *.apk/*.dex)
*.jar
*.aar
*.aab

# RE scratch + test/coverage artifacts
scratchpad/
.coverage
.coverage.*

# app-replica web UI: locally-overlaid VW strings/icons are never committed
ui/strings-local.json
calictl/webui/icons/
