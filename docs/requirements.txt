# Doc toolchain for the sphinx-needs requirement/traceability build.
# Build:  python -m venv .venv && .venv/bin/pip install -r requirements.txt
#         .venv/bin/python -m sphinx -b html   docs docs/_build/html
#         .venv/bin/python -m sphinx -b needs  docs docs/_build/needs
sphinx>=7,<8
sphinx-needs>=5,<6
sphinxcontrib-mermaid>=1,<2   # mermaid diagrams (client-side, .. mermaid:: + ```mermaid fences)
myst-parser>=2,<5             # the markdown docs (business-logic/, protocol.md, ...) on the site
furo>=2024.1                  # the theme — modern, dark-mode, announcement banner
