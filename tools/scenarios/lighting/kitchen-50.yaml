# Lighting was CRACKED from an HCI capture (2026-07-08): SET_BRIGHTNESS sets one zone and
# fills every other zone with 14 (leave-unchanged); calictl now reproduces the app byte-for-
# byte. This scenario stays as a regression/validation target — a fresh capture of the app
# setting Kitchen should diff to zero against calictl.
function: lighting
what: kitchen          # BrightnessLSeven
value: 5
control_char: "1501"
capture_label: kitchen-50
state: {ProfileNumber: 9}   # the active profile at capture time (echoed into the frame)
