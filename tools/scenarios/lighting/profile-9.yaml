# P4 validation — SET_PROFILE (Mode 16). Cracked from the 2026-07-08 capture; calictl reproduces
# it byte-for-byte (test_set_profile_is_mode_16). This scenario re-validates against a fresh
# capture: switching the active lighting profile to #9 in the app must diff to zero. char 1501.
# Expected calictl frame: 0910000000000000eeeeeeeeeeeeeeee  (ProfileNumber 9, Mode 16, zones 14).
#
# AT THE VAN: select lighting profile 9 in the app, capture, then:
#   python3 -m tools.capture_diff <capture.pcap> lighting/profile-9
function: lighting
what: profile
value: 9
control_char: "1501"
capture_label: lighting-profile-9
state: {ProfileNumber: 0}   # the profile active BEFORE the switch (echoed into build's base)
